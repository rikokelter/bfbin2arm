# Plot a two-arm one-stage ROPE equivalence design

Plot a two-arm one-stage ROPE equivalence design

## Usage

``` r
# S3 method for class 'bfbin2armrope2armdesign'
plot(
  x,
  type = c("overview", "operatingcharacteristics", "heatmap", "prior"),
  ...
)
```

## Arguments

- x:

  A `bfbin2armrope2armdesign` object.

- type:

  Plot type.

- ...:

  Currently unused.

## Value

Invisibly returns `x`.
