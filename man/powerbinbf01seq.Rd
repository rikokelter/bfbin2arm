% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/powerbinbf01seq.R
\name{powerbinbf01seq}
\alias{powerbinbf01seq}
\title{Bayesian and frequentist operating characteristics for a single-arm two-stage BF design}
\usage{
powerbinbf01seq(
  n1,
  n2,
  k,
  kf,
  p0,
  a0 = 1,
  b0 = 1,
  a1 = 1,
  b1 = 1,
  da0 = 1,
  db0 = 1,
  da1 = 1,
  db1 = 1,
  dp = NA_real_,
  type = c("point", "direction"),
  k_ce = NULL,
  grid_size = 801L
)
}
\arguments{
\item{n1}{Integer scalar. Interim sample size.}

\item{n2}{Integer scalar. Final sample size, with \code{n1 < n2}.}

\item{k}{Numeric scalar. Efficacy threshold on the \code{BF01} scale.}

\item{kf}{Numeric scalar. Futility threshold on the \code{BF01} scale.}

\item{p0}{Numeric scalar in \verb{(0, 1)}. Null response probability.}

\item{a0, b0}{Numeric scalars. Beta analysis-prior parameters under H0.}

\item{a1, b1}{Numeric scalars. Beta analysis-prior parameters under H1.}

\item{da0, db0}{Numeric scalars. Beta design-prior parameters under H0.}

\item{da1, db1}{Numeric scalars. Beta design-prior parameters under H1.}

\item{dp}{Optional numeric scalar in \verb{(0,1)}. If supplied, frequentist power
under \code{H1} is computed at \code{p = dp}.}

\item{type}{Character string. One of \verb{\\"point\\"} or \verb{\\"direction\\"}.}

\item{k_ce}{Optional numeric scalar greater than 1. Threshold for compelling
evidence in favour of \code{H0} on the \code{BF01} scale.}

\item{grid_size}{Integer number of grid points used for numerical averaging.}
}
\value{
A list with Bayesian and frequentist operating characteristics.
}
\description{
Computes naive fixed-sample and corrected two-stage operating characteristics
for a single-arm binomial design with one interim analysis for futility. The
Bayes factor is oriented as \code{BF01}, so efficacy corresponds to small values
(\code{BF01 <= k}) and futility corresponds to large values (\code{BF01 >= kf}).
}
\details{
Bayesian operating characteristics are computed under separate design priors:
\itemize{
\item for \code{type = "direction"}, Bayesian power averages over \code{p > p0} under the
H1 design prior truncated to \verb{(p0, 1]}, and Bayesian type-I error averages
over \code{p <= p0} under the H0 design prior truncated to \verb{[0, p0]};
\item for \code{type = "point"}, Bayesian power averages under the H1 design prior on
\verb{(0, 1)}, and Bayesian type-I error is evaluated at the point null \code{p = p0}.
}

If \code{dp} is supplied, additional frequentist power under \code{H1} is computed at
the fixed point alternative \code{p = dp}. Frequentist type-I error is computed at
\code{p = p0}.
}
