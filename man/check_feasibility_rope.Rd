% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_rope.R
\name{check_feasibility_rope}
\alias{check_feasibility_rope}
\title{Check asymptotic feasibility of a ROPE-based design}
\usage{
check_feasibility_rope(
  p0,
  delta,
  da0,
  db0,
  da1,
  db1,
  direction = c("equivalence", "noninferiority", "superiority"),
  target_power = NULL,
  target_type1 = NULL,
  verbose = TRUE
)
}
\arguments{
\item{p0}{Benchmark response probability.}

\item{delta}{ROPE half-width (equivalence), NI margin, or superiority margin.}

\item{da0, db0}{Design prior parameters under H0, Beta(da0, db0).}

\item{da1, db1}{Design prior parameters under H1, Beta(da1, db1).}

\item{direction}{Decision type: "equivalence", "noninferiority", or
"superiority".}

\item{target_power}{Target Bayesian predictive power under H1 (optional).}

\item{target_type1}{Target Bayesian predictive type-I error under H0
(optional).}

\item{verbose}{Logical; if TRUE (default), prints a human-readable
feasibility report.}
}
\value{
A list with components \code{power_ceiling}, \code{type1_floor},
\code{power_feasible} (logical or NA if no target given), and
\code{type1_feasible} (logical or NA if no target given).
}
\description{
Computes the asymptotic (n -> Inf) ceiling on Bayesian predictive power
under H1 and the asymptotic floor on Bayesian predictive type-I error
under H0, implied by the specified beta design priors and decision
boundary. These quantities provide a necessary (but not sufficient)
condition for the feasibility of a calibrated design, and can be used to
diagnose "No feasible design found" results from
\code{\link{design_singlearm_onestage_rope}} before running the full
numerical root-finding search. See Lemma 1 (non-inferiority /
superiority) and Lemma 2 (equivalence) in the accompanying manuscript.
}
\examples{
check_feasibility_rope(
  p0 = 0.30, delta = 0.10,
  da0 = 45, db0 = 105, da1 = 44, db1 = 36,
  direction = "superiority",
  target_power = 0.80, target_type1 = 0.10
)

}
