% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_singlearm_onestage_bf.R
\name{design_singlearm_onestage_bf}
\alias{design_singlearm_onestage_bf}
\title{Design or evaluate a one-stage single-arm Bayes factor trial}
\usage{
design_singlearm_onestage_bf(
  n_min,
  n_max,
  k,
  k_ce = NULL,
  p0,
  a0 = 1,
  b0 = 1,
  a1 = 1,
  b1 = 1,
  dp = NA_real_,
  da0 = 1,
  db0 = 1,
  da1 = 1,
  db1 = 1,
  type = c("point", "direction"),
  calibration = c("Bayesian", "frequentist", "hybrid", "full"),
  target_power = 0.8,
  target_type1 = 0.05,
  target_ce_h0 = 0,
  target_freq_power = 0.8,
  target_freq_type1 = 0.05,
  algorithm = c("optimal", "manual"),
  n = NULL,
  power_cushion = 0,
  sustain_n = 10L,
  ...
)
}
\arguments{
\item{n_min}{Integer. Minimum admissible sample size.}

\item{n_max}{Integer. Maximum admissible sample size.}

\item{k}{Numeric scalar greater than 0. Evidence threshold on the
\eqn{BF_{01}} scale for efficacy, used for power and type-I error.}

\item{k_ce}{Optional numeric scalar greater than 1. Threshold on the
\eqn{BF_{01}} scale used for CE(H0) / PCE(H0). Must be supplied when
\code{target_ce_h0 > 0}.}

\item{p0}{Numeric scalar in \eqn{(0,1)}. Null response probability.}

\item{a0, b0}{Positive numeric scalars. Beta analysis-prior parameters under
\eqn{H_0}.}

\item{a1, b1}{Positive numeric scalars. Beta analysis-prior parameters under
\eqn{H_1}.}

\item{dp}{Optional numeric scalar in \eqn{(0,1)}. Fixed point alternative
used for frequentist power calculations under \eqn{H_1}.}

\item{da0, db0}{Positive numeric scalars. Beta design-prior parameters under
\eqn{H_0}.}

\item{da1, db1}{Positive numeric scalars. Beta design-prior parameters under
\eqn{H_1}.}

\item{type}{Character string specifying the Bayes-factor test. One of
\code{"point"} or \code{"direction"}.}

\item{calibration}{Character string specifying the calibration mode. One of
\code{"Bayesian"}, \code{"frequentist"}, \code{"hybrid"}, or
\code{"full"}.}

\item{target_power}{Numeric scalar in \eqn{(0,1)}. Target corrected Bayesian
power.}

\item{target_type1}{Numeric scalar in \eqn{(0,1)}. Target corrected Bayesian
type-I error.}

\item{target_ce_h0}{Numeric scalar in \eqn{[0,1)}. Optional lower bound on
the corrected Bayesian probability of compelling evidence in favour of
\eqn{H_0}.}

\item{target_freq_power}{Numeric scalar in \eqn{(0,1)}. Target corrected
frequentist power at \code{dp}.}

\item{target_freq_type1}{Numeric scalar in \eqn{(0,1)}. Target corrected
frequentist type-I error at \eqn{p = p_0}.}

\item{algorithm}{Character string specifying whether the design should be
optimized or only evaluated.}

\item{n}{Optional integer sample size used when \code{algorithm = "manual"}.}

\item{power_cushion}{Optional additive cushion applied to the power targets
in the optimizer.}

\item{sustain_n}{Non-negative integer. A candidate design is considered
feasible only if the relevant operating characteristics satisfy their target
constraints at the candidate sample size and for the next \code{sustain_n}
larger sample sizes, subject to the search range. This also applies to the
CE(H0) constraint when \code{target_ce_h0 > 0}.}

\item{...}{Reserved for future extensions.}
}
\value{
An object of class \code{"singlearm_onestage_bf_design"}.
}
\description{
Calibrates or evaluates a one-stage single-arm Bayes factor design for a
binary endpoint.
}
\details{
The design uses the Bayes factor \eqn{BF_{01}}. Small values of
\eqn{BF_{01}} indicate evidence against \eqn{H_0}, so efficacy is concluded
when \eqn{BF_{01} \le k}. Large values indicate evidence in favour of
\eqn{H_0}, and the optional CE(H0) / PCE(H0) constraint is evaluated using
the separate threshold \code{k_ce}.

Analysis priors are specified separately under \eqn{H_0} and \eqn{H_1} via
\code{a0, b0, a1, b1}. Design priors are specified separately under
\eqn{H_0} and \eqn{H_1} via \code{da0, db0, da1, db1}.
}
