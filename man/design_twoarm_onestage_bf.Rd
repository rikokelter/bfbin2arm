% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_twoarm_onestage_bf.R
\name{design_twoarm_onestage_bf}
\alias{design_twoarm_onestage_bf}
\title{Design or evaluate a one-stage two-arm Bayes factor trial}
\usage{
design_twoarm_onestage_bf(
  n_min,
  n_max,
  k = 1/3,
  k_f = 3,
  test = c("BF01", "BF+0", "BF-0", "BF+-"),
  a_0_d = 1,
  b_0_d = 1,
  a_0_a = 1,
  b_0_a = 1,
  a_1_d = 1,
  b_1_d = 1,
  a_2_d = 1,
  b_2_d = 1,
  a_1_a = 1,
  b_1_a = 1,
  a_2_a = 1,
  b_2_a = 1,
  a_1_d_Hminus = 1,
  b_1_d_Hminus = 1,
  a_2_d_Hminus = 1,
  b_2_d_Hminus = 1,
  a_1_a_Hminus = 1,
  b_1_a_Hminus = 1,
  a_2_a_Hminus = 1,
  b_2_a_Hminus = 1,
  alloc1 = 0.5,
  alloc2 = 0.5,
  calibration = c("Bayesian", "frequentist", "hybrid", "full"),
  target_power = 0.8,
  target_type1 = 0.05,
  target_ce_h0 = 0,
  target_freq_power = 0.8,
  target_freq_type1 = 0.05,
  p1_grid = seq(0.01, 0.99, 0.02),
  p2_grid = seq(0.01, 0.99, 0.02),
  p1_power = NULL,
  p2_power = NULL,
  power_cushion = 0,
  sustain_n = 10L,
  report_freq_type1 = FALSE,
  algorithm = c("optimal", "manual"),
  n_total = NULL,
  progress = FALSE,
  ...
)
}
\arguments{
\item{n_min}{Integer. Minimum admissible total sample size.}

\item{n_max}{Integer. Maximum admissible total sample size.}

\item{k}{Numeric scalar greater than 0. Evidence threshold used for power and
type-I error.}

\item{k_f}{Numeric scalar greater than 1. Threshold used for CE(H0) /
PCE(H0).}

\item{test}{Character string, one of \code{"BF01"}, \code{"BF+0"},
\code{"BF-0"}, or \code{"BF+-"}.}

\item{a_0_d, b_0_d, a_0_a, b_0_a}{Shape parameters for design and analysis priors
under \eqn{H_0}.}

\item{a_1_d, b_1_d, a_2_d, b_2_d}{Shape parameters for design priors under
\eqn{H_1} or \eqn{H_+}.}

\item{a_1_a, b_1_a, a_2_a, b_2_a}{Shape parameters for analysis priors under
\eqn{H_1} or \eqn{H_+}.}

\item{a_1_d_Hminus, b_1_d_Hminus, a_2_d_Hminus, b_2_d_Hminus}{Optional design
priors under \eqn{H_-} for directional tests.}

\item{a_1_a_Hminus, b_1_a_Hminus, a_2_a_Hminus, b_2_a_Hminus}{Optional analysis
priors under \eqn{H_-} for directional tests.}

\item{alloc1, alloc2}{Fixed randomisation probabilities for arm 1 and arm 2.
Must be positive and sum to 1.}

\item{calibration}{Character string specifying the calibration mode. One of
\code{"Bayesian"}, \code{"frequentist"}, \code{"hybrid"}, or
\code{"full"}.}

\item{target_power}{Numeric scalar in \eqn{(0,1)}. Target corrected Bayesian
power.}

\item{target_type1}{Numeric scalar in \eqn{(0,1)}. Target corrected Bayesian
type-I error.}

\item{target_ce_h0}{Numeric scalar in \eqn{[0,1)}. Optional lower bound on
the corrected Bayesian probability of compelling evidence in favour of
\eqn{H_0} (or \eqn{H_-} for \code{test = "BF+-"}).}

\item{target_freq_power}{Numeric scalar in \eqn{(0,1)}. Target frequentist
power under \code{p1_power, p2_power}.}

\item{target_freq_type1}{Numeric scalar in \eqn{(0,1)}. Target frequentist
type-I error.}

\item{p1_grid, p2_grid}{Grids of true proportions used to compute supremum
frequentist type-I error.}

\item{p1_power, p2_power}{Optional true proportions used for frequentist power.}

\item{power_cushion}{Non-negative numeric scalar. Optional additive cushion
applied to the power targets during calibration.}

\item{sustain_n}{Non-negative integer. A candidate total sample size is
considered feasible only if the relevant target constraints hold at that
total sample size and for the next \code{sustain_n} larger total sample
sizes in the search range.}

\item{report_freq_type1}{Logical. If \code{TRUE}, compute and report the
frequentist type-I error for the final selected design even when the chosen
calibration mode does not use frequentist criteria. This additional
computation has no effect on the calibration itself. Defaults to
\code{FALSE}.}

\item{algorithm}{Character string specifying whether the design should be
optimized or only evaluated.}

\item{n_total}{Optional integer total sample size used when
\code{algorithm = "manual"}.}

\item{progress}{Logical; if \code{TRUE}, print simple progress information
during optimization.}

\item{...}{Reserved for future extensions.}
}
\value{
An object of class \code{"twoarm_onestage_bf_design"}.
}
\description{
Calibrates or evaluates a one-stage two-arm Bayes factor design for a binary
endpoint with fixed randomisation between the two arms.
}
\details{
The design uses one of the Bayes factor tests implemented in
\code{powertwoarmbinbf01()}. Small values of the relevant inverted Bayes
factor indicate evidence against the null, so efficacy is concluded when the
Bayes factor is below \code{k}. Large values indicate evidence in favour of
the null (or \eqn{H_-} for \code{test = "BF+-"}), and the optional
CE(H0) / PCE(H0) constraint is evaluated using \code{k_f}.
}
