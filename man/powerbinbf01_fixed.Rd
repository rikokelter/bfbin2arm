% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/powerbinbf01seq.R
\name{powerbinbf01_fixed}
\alias{powerbinbf01_fixed}
\title{Bayesian and frequentist operating characteristics for a fixed-sample
single-arm BF design}
\usage{
powerbinbf01_fixed(
  n,
  k,
  p0,
  a0 = 1,
  b0 = 1,
  a1 = 1,
  b1 = 1,
  da0 = 1,
  db0 = 1,
  da1 = 1,
  db1 = 1,
  dp = NA_real_,
  type = c("point", "direction"),
  k_ce = NULL,
  grid_size = 801L
)
}
\arguments{
\item{n}{Integer scalar. Total sample size.}

\item{k}{Numeric scalar. Efficacy threshold on the \code{BF01} scale.}

\item{p0}{Numeric scalar in \verb{(0, 1)}. Null response probability.}

\item{a0, b0}{Numeric scalars. Beta analysis-prior parameters under H0.}

\item{a1, b1}{Numeric scalars. Beta analysis-prior parameters under H1.}

\item{da0, db0}{Numeric scalars. Beta design-prior parameters under H0.}

\item{da1, db1}{Numeric scalars. Beta design-prior parameters under H1.}

\item{dp}{Optional numeric scalar in \verb{(0,1)}. If supplied, frequentist power
under \code{H1} is computed at \code{p = dp}.}

\item{type}{Character string. One of \verb{\\"point\\"} or \verb{\\"direction\\"}.}

\item{k_ce}{Optional numeric scalar greater than 1. Threshold for compelling
evidence in favour of \code{H0} on the \code{BF01} scale.}

\item{grid_size}{Integer number of grid points used for numerical averaging.}
}
\value{
A list with Bayesian and frequentist operating characteristics for the
fixed-sample design.
}
\description{
Computes operating characteristics for a genuine fixed-sample single-arm
binomial design with final efficacy decision based on \code{BF01 <= k}.
}
\details{
Bayesian operating characteristics are computed under separate design priors:
\itemize{
\item for \code{type = "direction"}, Bayesian power averages over \code{p > p0} under the
H1 design prior truncated to \verb{(p0, 1]}, Bayesian type-I error averages over
\code{p <= p0} under the H0 design prior truncated to \verb{[0, p0]}, and CE(H0) is
averaged over the same truncated H0 design prior;
\item for \code{type = "point"}, Bayesian power averages under the H1 design prior on
\verb{(0, 1)}, Bayesian type-I error is evaluated at the point null \code{p = p0},
and CE(H0) is also evaluated at \code{p = p0}.
}
}
