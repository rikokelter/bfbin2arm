% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_twoarm_onestage_rope.R
\name{design_twoarm_onestage_rope}
\alias{design_twoarm_onestage_rope}
\title{Calibrate a one-stage two-arm ROPE equivalence design for binary endpoints}
\usage{
design_twoarm_onestage_rope(
  nmin,
  nmax,
  nstep = 1L,
  delta,
  gammaeq = 0.9,
  gammadiff = gammaeq,
  analysis_prior_C = c(1, 1),
  analysis_prior_T = c(1, 1),
  design_prior_eq_C = NULL,
  design_prior_eq_T = NULL,
  design_prior_eq = NULL,
  design_prior_ne_C,
  design_prior_ne_T,
  targetpower = NULL,
  targettype1 = NULL,
  targetpce_h0 = NULL,
  sustainn = 1L,
  rel.tol = 1e-10,
  integration = c("adaptive", "quantile"),
  quad_nodes = 128L,
  calibration = c("Bayesian", "frequentist", "hybrid", "full"),
  freq_pC = NULL,
  freq_delta = NULL,
  targetfreqpower = NULL,
  targetfreqtype1 = NULL,
  freq_grid_n = 101L,
  freq_pC_range = c(0, 1),
  parallel = FALSE,
  ncores = 1L,
  parallel_backend = c("auto", "psock", "multicore", "none"),
  returngrid = TRUE,
  returnmatrices = FALSE,
  progress = interactive(),
  progress_every = 1L
)
}
\arguments{
\item{nmin, nmax}{Minimum and maximum per-arm sample size.}

\item{nstep}{Positive per-arm sample-size increment.}

\item{delta}{ROPE half-width on the risk-difference scale.}

\item{gammaeq}{Posterior threshold for equivalence.}

\item{gammadiff}{Posterior threshold for meaningful non-equivalence.}

\item{analysis_prior_C, analysis_prior_T}{Length-two Beta analysis-prior shapes.}

\item{design_prior_eq_C, design_prior_eq_T}{Legacy equivalence-prior Beta kernels.}

\item{design_prior_eq}{Optional joint baseline-risk/risk-difference design prior.}

\item{design_prior_ne_C, design_prior_ne_T}{Non-equivalence-prior Beta kernels.}

\item{targetpower}{Target Bayesian predictive probability of declaring
practical equivalence under the equivalence design distribution \code{G1}.}

\item{targettype1}{Maximum Bayesian predictive probability of falsely
declaring practical equivalence under the non-equivalence design
distribution \code{G0}.}

\item{targetpce_h0}{Optional minimum Bayesian predictive probability of
compelling evidence for meaningful non-equivalence under \code{G0}. More
precisely, this is the predictive probability of declaring meaningful
difference, i.e. the posterior probability of \code{abs(pT - pC) >= delta}
is at least \code{gammadiff}. If supplied, this criterion is enforced for
\code{"Bayesian"}, \code{"hybrid"}, and \code{"full"} calibration.}

\item{sustainn}{Number of consecutive feasible evaluated sample sizes.}

\item{rel.tol}{Adaptive-integration relative tolerance.}

\item{integration}{Either \code{"adaptive"} or \code{"quantile"}.}

\item{quad_nodes}{Number of Gauss-Legendre nodes for quantile quadrature.}

\item{calibration}{One of \code{"Bayesian"}, \code{"frequentist"}, \code{"hybrid"}, or \code{"full"}.}

\item{freq_pC}{Control response probability for frequentist pointwise power.}

\item{freq_delta}{Treatment-control difference for frequentist pointwise power.}

\item{targetfreqpower}{Target pointwise frequentist equivalence power.}

\item{targetfreqtype1}{Maximum boundary-grid frequentist type-I error.}

\item{freq_grid_n}{Boundary-grid size.}

\item{freq_pC_range}{Control-risk interval for boundary-grid evaluation.}

\item{parallel}{Logical; if \code{TRUE}, evaluate candidate per-arm sample sizes
in parallel.}

\item{ncores}{Number of workers if \code{parallel = TRUE}.}

\item{parallel_backend}{Parallel backend. \code{"auto"} and \code{"psock"} use a
PSOCK cluster, which is supported on Windows, macOS, and Linux.
\code{"none"} evaluates candidates sequentially. \code{"multicore"} is retained
for compatibility but currently resolves to \code{"psock"} because forked
processes can be unsafe with threaded numerical libraries.}

\item{returngrid}{Return the full operating-characteristic grid.}

\item{returnmatrices}{Retain matrices for the selected design.}

\item{progress}{Logical; if \code{TRUE}, show a progress bar during sequential
evaluation. Progress display is disabled for parallel evaluation.}

\item{progress_every}{Positive integer specifying the interval, in evaluated
candidate sample sizes, at which sequential progress is updated.}
}
\value{
An object of class \code{"bfbin2armrope2armdesign"}.
}
\description{
Calculates equal-allocation sample sizes for a parallel-group one-stage
equivalence study with binary endpoints. The treatment effect is the risk
difference \code{pT - pC}. Equivalence is declared when the posterior probability
of \code{abs(pT - pC) < delta} is at least \code{gammaeq}; meaningful non-equivalence
is declared when the posterior probability outside the ROPE is at least
\code{gammadiff}; otherwise the decision is inconclusive.
}
