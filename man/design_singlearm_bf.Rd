% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_singlearm_bf.R
\name{design_singlearm_bf}
\alias{design_singlearm_bf}
\title{Design or evaluate a single-arm two-stage Bayes factor trial}
\usage{
design_singlearm_bf(
  n1_min,
  n2_max,
  k,
  k_f,
  p0,
  a0 = 1,
  b0 = 1,
  a1 = 1,
  b1 = 1,
  dp = NA_real_,
  da0 = 1,
  db0 = 1,
  da1 = 1,
  db1 = 1,
  type = c("point", "direction"),
  calibration = c("Bayesian", "frequentist", "hybrid", "full"),
  target_power = 0.8,
  target_type1 = 0.05,
  target_ce_h0 = 0,
  target_freq_power = 0.8,
  target_freq_type1 = 0.05,
  algorithm = c("optimal", "manual"),
  interim = NULL,
  final = NULL,
  power_cushion = 0,
  ...
)
}
\arguments{
\item{n1_min}{Integer. Minimum admissible interim sample size.}

\item{n2_max}{Integer. Maximum admissible final sample size.}

\item{k}{Numeric scalar greater than 0. Efficacy threshold on the
\eqn{BF_{01}} scale.}

\item{k_f}{Numeric scalar greater than 1. Futility threshold on the
\eqn{BF_{01}} scale.}

\item{p0}{Numeric scalar in \eqn{(0,1)}. Null response probability.}

\item{a0, b0}{Positive numeric scalars. Beta analysis-prior parameters under
\eqn{H_0}.}

\item{a1, b1}{Positive numeric scalars. Beta analysis-prior parameters under
\eqn{H_1}.}

\item{dp}{Optional numeric scalar in \eqn{(0,1)}. Fixed point alternative
used for frequentist power calculations under \eqn{H_1}.}

\item{da0, db0}{Positive numeric scalars. Beta design-prior parameters under
\eqn{H_0}.}

\item{da1, db1}{Positive numeric scalars. Beta design-prior parameters under
\eqn{H_1}.}

\item{type}{Character string specifying the Bayes-factor test. One of
\code{"point"} or \code{"direction"}.}

\item{calibration}{Character string specifying the calibration mode. One of
\code{"Bayesian"}, \code{"frequentist"}, \code{"hybrid"}, or
\code{"full"}.}

\item{target_power}{Numeric scalar in \eqn{(0,1)}. Target corrected Bayesian
power.}

\item{target_type1}{Numeric scalar in \eqn{(0,1)}. Target corrected Bayesian
type-I error.}

\item{target_ce_h0}{Numeric scalar in \eqn{[0,1)}. Optional lower bound on
the corrected Bayesian probability of compelling evidence in favour of
\eqn{H_0}.}

\item{target_freq_power}{Numeric scalar in \eqn{(0,1)}. Target corrected
frequentist power at \code{dp}.}

\item{target_freq_type1}{Numeric scalar in \eqn{(0,1)}. Target corrected
frequentist type-I error at \eqn{p = p_0}.}

\item{algorithm}{Character string specifying whether the design should be
optimized or only evaluated.}

\item{interim}{Optional integer interim sample size used when
\code{algorithm = "manual"}.}

\item{final}{Optional integer final sample size used when
\code{algorithm = "manual"}.}

\item{power_cushion}{Optional additive cushion applied only in the
fixed-sample anchor search of the first optimization step. This can be
useful because introducing an interim futility analysis typically reduces
corrected power relative to the fixed-sample anchor.}

\item{...}{Reserved for future extensions.}
}
\value{
An object of class \code{"singlearm_bf_design"}.
}
\description{
Calibrates or evaluates a single-arm two-stage Bayes factor design for a
binary endpoint with one interim analysis for futility.
}
\details{
The design uses the Bayes factor \eqn{BF_{01}}. Small values of
\eqn{BF_{01}} indicate evidence against \eqn{H_0}, so final efficacy is
concluded when \eqn{BF_{01} \\le k}. Large values indicate evidence in favour
of \eqn{H_0}, so interim futility is concluded when
\eqn{BF_{01} \\ge k_f}.

Analysis priors are specified separately under \eqn{H_0} and \eqn{H_1} via
\code{a0, b0, a1, b1}. Design priors are specified separately under
\eqn{H_0} and \eqn{H_1} via \code{da0, db0, da1, db1}.
}
